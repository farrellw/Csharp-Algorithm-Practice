namespace Practice
{

    public class Palindrome
    {
        public static bool checker(string stringOne, string stringTwo)
        {
            return true;
        }

        public static void sorter(char[] arrayOfChars)
        {
            //char[] sortedArray = {}
            //arrayOfChars[]
            //Array.sort(arrayOfChars);
            //return new string(arrayOfChars);
        }
    }
}